[package]
name = "taskbook-common"
version = "1.0.0"
edition = "2021"
description = "Shared types for taskbook-rs"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
thiserror = "1"
aes-gcm = "0.10"
rand = "0.8"
