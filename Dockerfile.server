FROM debian:bookworm-slim

ARG TARGETARCH

RUN apt-get update && apt-get install -y ca-certificates && rm -rf /var/lib/apt/lists/*

COPY binaries/${TARGETARCH}/tb-server /usr/local/bin/tb-server

ENV TB_HOST=0.0.0.0
ENV TB_PORT=8080

EXPOSE 8080

CMD ["tb-server"]
